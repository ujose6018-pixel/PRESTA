rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regla simplificada para la colección 'loans'
    match /loans/{loanId} {
      allow read, write: if request.time < timestamp.date(2030, 5, 9);
    }
  }
}
